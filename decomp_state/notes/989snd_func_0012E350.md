# func_0012E350 (0x12E350, 20 bytes) — BLOCKED: dead compiler-emitted tail

NOT a function. Unreachable 20-byte fragment immediately after the
byte-matched parent `snd_PlaySoundVolPanPMPB` (0x12E308, 0x48 bytes,
frame 0x30, code/989snd/ee/989snd_post.c:57). Seventh member of the
15-fragment 989snd dead-tail family; family analysis and cross-game
evidence in notes/989snd_func_0012E078.md.

```
0x12E350: addiu sp,sp,0x10   (0x27BD0010)
0x12E354: nop                (0x00000000)
0x12E358: addiu sp,sp,0x10   (0x27BD0010)
0x12E35C: nop                (0x00000000)
0x12E360: addiu sp,sp,0x10   (0x27BD0010)
0x12E364: nop  (alignment padding, OUTSIDE the fragment)
```

The parent completes its balanced epilogue at 0x12E34C (`lq ra,32(sp)`
at 0x12E344, `jr ra` at 0x12E348, `addiu sp,sp,0x30` in the delay slot).
The next function `snd_StopSound` (matched, frame 0x20) starts 8-aligned
at 0x12E368. The fragment has no prologue, no return, no branch — it
cannot be a function, and nothing reaches it. Unlike the 0012E198
sibling, these units (0x10 x3) do not equal the parent's 0x30 frame;
the family note established unit sizes are not derivable from the
decompiled body.

## Deadness (verified 2026-09-19, tools/deadness_scan.py)

- 0 jal/j/relative-branch targets and 0 32/64-bit data words for
  0x12E350 across core.text (0x112380+0x1D0B8), .text
  (0x1E8D00+0x54660), and all data sections.
- Ghidra (headless): no function at or containing 0x12E350; zero xrefs.

## Escalation

**last-resort GPT-5.6 Sol used (2026-09-19)** on this exact target
(full dossier: raw words, parent/next-function boundaries, deadness
scans, family + Deadlocked .mdebug evidence, t1-t10/p1-p5 probe
results, prior sibling verdicts). Verdict: the blocker is missing
compiler RTL, not scheduling/signatures/assembler expansion. EGC
generates positive sp adjustments only from reachable frame
restoration; the matched parent compiles all 72 bytes and emits no
tail; unreachable statements after a return are deleted; a standalone
C function must emit a terminator; folding into the parent or
snd_StopSound is impossible (all callers enter at 0x12E308 /
0x12E368, whose matched prologues begin there); flags and inline asm
cannot synthesize the bytes (byte-injection equivalent to the
fallback). No materially distinct probe remains.

## Resolution

Retain `INCLUDE_ASM("code/_generated/nonmatchings/989snd/ee/989snd_post",
func_0012E350)` to supply the 20 bytes and preserve full-ELF parity.
Blocked as a dead tail, not a function. Remaining family siblings take
the same treatment (deadness verification per sibling via
tools/deadness_scan.py, last-resort per the hard rule, block).
