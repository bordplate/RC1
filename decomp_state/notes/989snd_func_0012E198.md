# func_0012E198 (0x12E198, 12 bytes) — BLOCKED: dead compiler-emitted tail

NOT a function. Unreachable 12-byte fragment immediately after the
byte-matched parent `snd_BankLoadFromEE_CB` (0x12E088, 0x110 bytes,
frame 0x50, code/989snd/ee/989snd_bankload.c:39). Second member of the
15-fragment 989snd dead-tail family; family analysis and cross-game
evidence in notes/989snd_func_0012E078.md.

```
0x12E198: addiu sp,sp,0x50   (0x27BD0050)
0x12E19C: nop                (0x00000000)
0x12E1A0: addiu sp,sp,0x50   (0x27BD0050)
0x12E1A4: nop  (alignment padding, OUTSIDE the fragment)
```

The parent completes its balanced epilogue at 0x12E194
(`lq ra/s8/s7/s6` at 0x12E180-18C, `jr ra` at 0x12E190, `addiu
sp,sp,0x50` in the delay slot). The next function
`snd_ResolveBankXREFS` (matched, 0x12E1A8) starts 8-aligned after the
fragment. The fragment has no prologue, no return, no branch — it
cannot be a function. Unlike the family head 0012E078 (units 0x60,0x50),
this sibling's units (0x50,0x50) equal the parent's 0x50 frame, but the
family note established unit sizes are not derivable from the decompiled
body.

## Deadness (verified 2026-09-18, tools/deadness_scan.py)

New raw-ELF scanner (committed separately): scans core.text and .text
for jal/j/relative-branch instructions targeting the address, plus all
non-text sections for 32/64-bit words equal to it.

- 0 jal/j targeting 0x12E198 in core.text (0x112380+0x1D0B8) or
  .text (0x1E8D00+0x54660).
- 0 relative branches (beq/bne/blez/bgtz/beql/bnel/blezl/bgtzl/
  beqz/bnez) targeting 0x12E198.
- 0 32/64-bit words equal to 0x12E198 in any non-text section
  (core.data, core.rdata, core.lit, .lit, .data, lvl.* tables).
- Ghidra (headless): no function at or containing 0x12E198; zero xrefs.
- Scanner sanity: on the adjacent live symbol 0x12E1A8 it finds exactly
  the 6 jal xrefs Ghidra reports.

## Escalation

**last-resort GPT-5.6 Sol used (2026-09-18)** on this exact target
(full dossier: bytes, layout, deadness scans, family + Deadlocked
evidence, t1-t10/p1-p5 probe results, family-head verdict). Verdict:
no acceptable C/C++ form exists. The matched parent is itself the
strongest exact test for the coincidental 0x50-frame case: it compiles
all 272 bytes at 0x12E088-0x12E197 and emits no tail. Merging the
fragment into the parent requires EGC to retain two post-return stack
adjustments (it deletes unreachable RTL and emits only the live
epilogue restoration); a standalone C function must emit a
terminator; folding it into snd_ResolveBankXREFS is impossible (all
six callers enter at 0x12E1A8, whose matched prologue begins there);
scheduler/address-splitting/optimization/assembler flags cannot
synthesize missing RTL; inline asm or initialized instruction words
are byte-injection equivalent to the INCLUDE_ASM fallback. No
materially distinct probe remains to run.

## Resolution

Retain `INCLUDE_ASM("code/_generated/nonmatchings/989snd/ee/989snd_post",
func_0012E198)` to supply the 12 bytes and preserve full-ELF parity.
Blocked as a dead tail, not a function. Remaining family siblings take
the same treatment (deadness verification per sibling via
tools/deadness_scan.py, last-resort per the hard rule, block).
