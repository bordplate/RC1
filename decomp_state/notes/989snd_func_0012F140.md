# func_0012F140 (0x12F140, 4 bytes) — BLOCKED: dead compiler-emitted tail

NOT a function. Unreachable 4-byte fragment immediately after the
byte-matched parent `snd_StartMovieSound` (0x12F108, 0x38 bytes, frame
0x30, code/989snd/ee/989snd_post.c:470). Fifteenth (final) member of the
15-fragment 989snd dead-tail family; family analysis, cross-game
Deadlocked `.mdebug` gap evidence, and local-EGC t1-t10/p1-p5 probes in
notes/989snd_func_0012E078.md.

```
0x12F140: addiu sp,sp,0x10   (0x27BD0010)
0x12F144: nop  (alignment padding, OUTSIDE the fragment)
```

A single unit `addiu sp,sp,0x10; nop`. No prologue, no return, no branch
— it cannot be a function. The parent completes its balanced epilogue at
0x12F13F (`lq ra`, `jr ra` at 0x12F138, `addiu sp,sp,0x30` in the delay
slot at 0x12F13C). The next function `snd_UpdateMovieADPCM` (matched,
0x12F148) starts 8-aligned after the fragment. The unit (0x10) does NOT
equal the parent's 0x30 frame and is not derivable from the decompiled
body: the parent is a 5-arg `snd_SendIOPCommandAndWait` wrapper whose
live bytes EGC already reproduces exactly, and the identical wrapper
shapes of the surrounding siblings (snd_CloseMovieSound,
snd_ResetMovieSound) carry different or absent tails.

## Deadness (verified 2026-09-20, tools/deadness_scan.py)

Full EE branch map (j/jal; beq/beqz/b, bne/bnez, blez, bgtz; regimm
bltz/bgez/bltzl/bgezl/bgezal rt 0/1/2/3/17; COP0 bc0f/bc0t and COP1
bc1f/bc1t/bc1fl/bc1tl fmt=2; beql/bnel/blezl/bgtzl) plus 32/64-bit
data words in every non-text section:

- `python3 tools/deadness_scan.py 0x12F140` -> 0 reference(s): no
  jal/j, no relative branch of any EE encoding targeting 0x12F140 in
  core.text or .text, and no 32/64-bit word equal to it in any other
  section (no jump-table entry).
- Scanner positive controls: 0x12F108 (parent) finds its one jal at
  0x23ACF0; 0x12F148 (next function) finds its one jal at 0x23AF9C —
  both matching Ghidra, confirming the scanner sees the real references
  while the fragment has none.
- Ghidra (headless): no function at or containing 0x12F140; the parent
  body ends exactly at 0x12F13F; zero xrefs to the fragment.
- The matched parent compiles all 0x38 bytes at 0x12F108-0x12F13F and
  emits no tail; full-ELF cmp passes with the INCLUDE_ASM supplying the
  0x4 fragment.

## Escalation

**last-resort GPT-5.6 Sol used (2026-09-20)** on this exact target
(full dossier: raw word, balanced matched-parent epilogue, deadness
scans with two positive controls, family table + Deadlocked `.mdebug`
gap evidence, t1-t10/p1-p5 local EGC probes, the four prior movie-sound
sibling verdicts). Verdict: no credible C/declaration/qualifier/flag/
assembler route remains. The blocker is missing RTL, not scheduling or
allocation: the parent already emits a balanced -0x30/+0x30 frame and
matches all 14 words; synthesizing a second post-return stack restoration
requires changing the frame or reachable control flow, which EGC's
dead-code elimination removes and which scheduler / address-splitting /
optimization / -G / GNU-vs-SN choices cannot create. A standalone C
function necessarily gains a terminator and cannot fall through into
`snd_UpdateMovieADPCM`; inline/file-scope asm would be byte-injection
equivalent to the retained INCLUDE_ASM.

## Resolution

Retain `INCLUDE_ASM("code/_generated/nonmatchings/989snd/ee/989snd_post",
func_0012F140)` to supply the 4 bytes and preserve full-ELF parity.
Blocked as a dead tail, not a function. This was the last member of the
15-fragment 989snd dead-tail family.
