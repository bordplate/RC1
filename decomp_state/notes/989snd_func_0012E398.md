# func_0012E398 (0x12E398, 28 bytes) — BLOCKED: dead compiler-emitted tail

NOT a function. Unreachable 28-byte fragment immediately after the
byte-matched parent `snd_StopSound` (0x12E368, 48 bytes, frame 0x20,
code/989snd/ee/989snd_post.c:70). Eighth member of the 15-fragment 989snd
dead-tail family; family analysis and cross-game evidence in
notes/989snd_func_0012E078.md.

```
0x12E398: addiu sp,sp,0x40   (0x27BD0040)
0x12E39C: nop                (0x00000000)
0x12E3A0: addiu sp,sp,0x40   (0x27BD0040)
0x12E3A4: nop                (0x00000000)
0x12E3A8: addiu sp,sp,0x20   (0x27BD0020)
0x12E3AC: nop                (0x00000000)
0x12E3B0: addiu sp,sp,0x30   (0x27BD0030)
0x12E3B4: nop  (alignment padding, OUTSIDE the fragment)
```

The parent completes its balanced epilogue at 0x12E394 (`lq ra,16(sp)`
at 0x12E38C, `jr ra` at 0x12E390, `addiu sp,sp,0x20` in the delay slot).
The next function `snd_StopAllSounds` (matched, frame 0x10) starts 8-aligned
at 0x12E3B8. The fragment has no prologue, no return, no branch — it
cannot be a function, and nothing reaches it. The units (0x40, 0x40,
0x20, 0x30) do not equal the parent's 0x20 frame; the family note
established unit sizes are not derivable from the decompiled body.

## Deadness (verified 2026-09-19, tools/deadness_scan.py)

- 0 jal/j/relative-branch targets and 0 32/64-bit data words for
  0x12E398 across core.text (0x112380+0x1D0B8), .text
  (0x1E8D00+0x54660), and all data sections.
- Scanner sanity: the same scanner finds exactly the 2 jal xrefs for each
  adjacent live function (0x12E3B8 snd_StopAllSounds: jal at 0x204670 and
  0x22DCE8; 0x12E368 snd_StopSound: jal at 0x2160FC and 0x22D4E4), so the
  0 for 0x12E398 is real.
- Ghidra (headless): no function at or containing 0x12E398; zero xrefs.

## Escalation

**last-resort GPT-5.6 Sol used (2026-09-19)** on this exact target
(full dossier: raw words, parent/next-function boundaries, deadness scans,
family + Deadlocked .mdebug evidence, t1-t10/p1-p5 probe results, prior
sibling verdicts). Verdict: no credible C/codegen route remains. EGC
emits positive sp adjustments only from reachable frame-restoration RTL;
the matched parent compiles all 48 bytes and emits no tail; unreachable
statements after a return are deleted; a standalone C function must emit a
terminator; folding into the parent or snd_StopAllSounds is impossible
(all callers enter at 0x12E368 / 0x12E3B8, whose matched prologues begin
there); scheduling/address-splitting flags and the assembler cannot
synthesize absent RTL (the bytes are concrete addiu/nop, not expandable
pseudos); file-scope asm or a .text word array is byte-injection equivalent
to the fallback. Note: production 989snd_post.o uses the GNU assembler
override (`-Wa,-EL -Wa,-Icode/include`), which does not affect this
blocker (raw addiu/nop are not macro-expanded).

## Resolution

Retain `INCLUDE_ASM("code/_generated/nonmatchings/989snd/ee/989snd_post",
func_0012E398)` to supply the 28 bytes and preserve full-ELF parity.
Blocked as a dead tail, not a function. Remaining family siblings take
the same treatment (deadness verification per sibling via
tools/deadness_scan.py, last-resort per the hard rule, block).
