# func_00207A08 (code/game/menu.cpp)

- Original: 8 bytes at file offset `0x108988` in `assets/boot_elf.elf`:
  `0800e003 01000224` = `jr $ra; addiu $v0,$0,1` (returns 1).
- Splat vram address: `0x00207A08` (menu subsegment starts at file offset
  `0x1078f8`; function sits at object-relative offset `0x1090`).
- Ghidra shows no standalone function there; the stub is folded into the tail of
  the preceding function range. A DATA xref from `0x001a0014` (function-pointer
  table) points at it, so it is a callback entry in a table of predicate-like
  functions (neighbours: func_002079C8 byte-flag test, func_00207A10 identical
  `return 1`, func_00207A18 distance < 35.0f check).
- Replacement:
  ```cpp
  extern "C" int func_00207A08(void) {
      return 1;
  }
  ```
- Verification (mechanical):
  - `nm`/symtab of `build/code/game/menu.o`: symbol at .text+0x1090, st_size=8.
  - Candidate bytes in object: `0800e00301000224` == original slice.
  - Neighbours contiguous as original (func_002079D8 ends at 0x1090,
    func_00207A10 starts at 0x1098); menu .text total = 0x26b8 == subsegment len.
  - `make` + `cmp build/boot_elf.elf assets/boot_elf.elf` passes.
