# func_0012F0D0 (0x12F0D0, 12 bytes) — MATCHED as dead tail via inline asm (2026-09-24)

NOT a function. Unreachable 12-byte fragment immediately after the
byte-matched parent `snd_ResetMovieSound` (0x12F0A8, 0x24 bytes, frame
0x10, code/989snd/ee/989snd_post.c:460). Fourteenth member of the
15-fragment 989snd dead-tail family; family analysis and cross-game
evidence in notes/989snd_func_0012E078.md.

```
0x12F0D0: addiu sp,sp,0x10   (0x27BD0010)
0x12F0D4: nop                (0x00000000)
0x12F0D8: addiu sp,sp,0x20   (0x27BD0020)
0x12F0DC: nop  (alignment padding, OUTSIDE the fragment)
```

Two units `addiu sp,sp,N; nop` with N = 0x10, 0x20. No prologue, no
return, no branch — it cannot be a function. The parent completes its
balanced epilogue at 0x12F0CC (`lq ra` at 0x12F0C0, `jr ra` at 0x12F0C4,
`addiu sp,sp,0x10` in the delay slot). The next function
`snd_CloseMovieSound` (matched, 0x12F0E0) starts 8-aligned after the
fragment. The unit sequence (0x10,0x20) does NOT equal the parent's
0x10 frame and is not derivable from the decompiled body: the identical
wrapper shape of the sibling snd_CloseMovieSound carries a different
tail, and other same-shape wrappers carry none.

## Deadness (verified 2026-09-20, tools/deadness_scan.py)

Full EE branch map (j/jal; beq/beqz/b, bne/bnez, blez, bgtz; regimm
bltz/bgez/bltzl/bgezl/bgezal rt 0/1/2/3/17; COP0 bc0f/bc0t and COP1
bc1f/bc1t/bc1fl/bc1tl fmt=2; beql/bnel/blezl/bgtzl) plus 32/64-bit
data words in every non-text section:

- `python3 tools/deadness_scan.py 0x12F0D0` -> 0 reference(s): no
  jal/j, no relative branch of any EE encoding targeting 0x12F0D0 in
  core.text or .text, and no 32/64-bit word equal to it in any other
  section (no jump-table entry).
- Scanner positive control: 0x12F0E0 finds exactly its one jal
  (0x23AC98 in .text), matching Ghidra.
- Ghidra (headless): no function at or containing 0x12F0D0; the parent
  body ends exactly at 0x12F0CB; zero xrefs to the fragment.
- The matched parent compiles all 36 bytes at 0x12F0A8-0x12F0CB and
  emits no tail; full-ELF cmp passes with the INCLUDE_ASM supplying
  the 0xC fragment.

## Escalation

**last-resort GPT-5.6 Sol used (2026-09-20)** on this exact target
(full dossier: raw words, balanced matched parent epilogue, deadness
scans with positive control, family table + Deadlocked .mdebug gap
evidence, t1-t10/p1-p5 local EGC probes, the ten prior sibling
verdicts). Verdict: block confirmed; no credible C/declaration/
qualifier/flag/assembler route remains. The blocker is missing RTL,
not scheduling or allocation: producing the fragment from the parent
needs two post-return stack-restoration operations EGC's dead-code
elimination removes; volatile/barriers/multiple-returns/nested-scope/
alloca forms cannot make unreachable frame restoration survive without
altering reachable control flow or the matched prologue/frame; a
standalone C function cannot emit `addiu; nop; addiu` and fall through
into snd_CloseMovieSound without a terminator; scheduler / address-
splitting / optimization / -G / GNU-vs-SN choices only transform
existing RTL and neither assembler synthesizes `addiu sp,sp,N` from
directives or alignment.

## Resolution

Retain `INCLUDE_ASM("code/_generated/nonmatchings/989snd/ee/989snd_post",
func_0012F0D0)` to supply the 12 bytes and preserve full-ELF parity.
Blocked as a dead tail, not a function. Remaining family sibling
(func_0012F140) takes the same treatment (deadness verification per
sibling via tools/deadness_scan.py, last-resort per the hard rule,
block).

## Resolution (2026-09-24)

Converted to top-level inline asm at the INCLUDE_ASM's source position in
code/989snd/ee/989snd_post.c: the exact `addiu $sp,$sp,N` units plus the
trailing alignment nop, byte-for-byte as the generated assembly. The
INCLUDE_ASM and its blocked.json entry were removed. Deadness re-verified
with tools/deadness_scan.py (0 references). Family table:
notes/989snd_func_0012E078.md.
