# func_0012EF58 (0x12EF58, 12 bytes) — BLOCKED: dead compiler-emitted tail

NOT a function. Unreachable 12-byte fragment immediately after the
byte-matched parent `snd_StreamSafeCdCallback` (0x12EF28, 0x30 bytes,
frame 0x10, code/989snd/ee/989snd_post.c:407). Member of the
15-fragment 989snd dead-tail family; family analysis and cross-game
evidence in notes/989snd_func_0012E078.md.

```
0x12EF58: addiu sp,sp,0x20   (0x27BD0020)
0x12EF5C: nop                (0x00000000)
0x12EF60: addiu sp,sp,0x20   (0x27BD0020)
0x12EF64: nop  (alignment padding, OUTSIDE the fragment)
```

The parent completes its balanced epilogue at 0x12EF54
(`lq $31` at 0x12EF4C, `jr $31` at 0x12EF50, `addiu sp,sp,0x10` in
the delay slot). The next function `snd_SetReverbEx` (matched) starts
8-aligned at 0x12EF68. The fragment has no prologue, no return, no
branch — it cannot be a function. The units (0x20,0x20) do NOT equal
the parent's 0x10 frame, consistent with the family rule that unit
sizes are not derivable from the decompiled body.

## Deadness (verified 2026-09-20, tools/deadness_scan.py)

Scan with the corrected full EE branch map (j/jal; beq/beqz/b,
bne/bnez, blez, bgtz; regimm bltz/bgez/bltzl/bgezl/bgezal selected by
rt 0/1/2/3/17; COP0 bc0f/bc0t and COP1 bc1f/bc1t/bc1fl/bc1tl
selected by fmt=2; beql/bnel/blezl/bgtzl) plus 32/64-bit data words
in every non-text section:

- `python3 tools/deadness_scan.py 0x12EF58` -> 0 reference(s):
  no jal/j, no relative branch of any EE encoding targeting
  0x12EF58 in core.text or .text, and no 32/64-bit word equal to it
  in any other section (no jump-table entry).
- Ghidra (headless): no function at or containing 0x0012EF58; the
  parent body ends exactly at 0x0012EF57; zero xrefs.
- Scanner positive controls (all newly covered types fire): bgez
  0x12DAE8 -> 0x12DB1C, bgezal 0x21835C/0x218484/0x21863C ->
  0x21880C, bc0f 0x217D38 -> 0x217D24, bc1f/bc1tl/bc1fl 0x207820/
  0x20783C/0x201B28 -> 0x207844/0x201B68, bc1t 0x206EBC -> 0x206F34.

## Escalation

**last-resort GPT-5.6 Sol used (2026-09-20)** on this exact target
(full dossier: raw bytes, matched parent epilogue, deadness scans,
family table + Deadlocked .mdebug gap evidence, t1-t10/p1-p5 probe
results, flag trials). Verdict: no credible C/C++, declaration,
qualifier, compiler flag, or assembler-mode route remains. The blocker
is missing RTL, not scheduling or allocation: the local compiler
emits no post-epilogue stack-restoration RTL; ordinary C produces
positive sp adjustments only as live frame restoration (which would
alter the byte-matched parent); unreachable statements after return
are deleted; a standalone C function cannot consist of three words
with no terminator falling through into snd_SetReverbEx; scheduler /
address-splitting / optimization / assembler flags only transform
existing RTL; GNU-vs-SN assembly offers nothing (addiu/nop are
concrete instructions). The only mechanically exact constructs are
literal byte injection (file-scope `.word` asm), which is
byte-injection equivalent to the retained INCLUDE_ASM fallback. It
also supplied the deadness-note opcode correction implemented in
tools/deadness_scan.py (committed separately).

## Resolution

Retain `INCLUDE_ASM("code/_generated/nonmatchings/989snd/ee/989snd_post",
func_0012EF58)` to supply the 12 bytes and preserve full-ELF parity.
Blocked as a dead tail, not a function. Remaining family siblings
(func_0012F020, func_0012F0D0, func_0012F140) take the same
treatment (deadness verification per sibling via
tools/deadness_scan.py, last-resort per the hard rule, block).
