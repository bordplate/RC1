# func_0012E2F8 (0x12E2F8, 12 bytes) — MATCHED as dead tail via inline asm (2026-09-24)

NOT a function. Unreachable 12-byte fragment immediately after the
byte-matched parent `snd_SetGroupVoiceRange` (0x12E2B8, 0x3C bytes,
frame 0x20, code/989snd/ee/989snd_post.c:47). Fourth blocked member of
the 15-fragment 989snd dead-tail family; family analysis and
cross-game evidence in notes/989snd_func_0012E078.md.

```
0x12E2F8: addiu sp,sp,0x30   (0x27BD0030)
0x12E2FC: nop                (0x00000000)
0x12E300: addiu sp,sp,0x30   (0x27BD0030)
0x12E304: nop  (alignment padding, OUTSIDE the fragment)
```

The parent completes its balanced epilogue at 0x12E2F3 (`lq ra,0x10(sp)`
@0x12E2E8, `jr ra` @0x12E2EC, `addiu sp,sp,0x20` in the delay slot
@0x12E2F0, alignment nop @0x12E2F4). The next live symbol
`snd_PlaySoundVolPanPMPB` (matched, 0x12E308) starts 8-aligned after the
fragment. The fragment has no prologue, no return, no branch — it cannot
be a function. The units (0x30,0x30) do not equal the parent's 0x20
frame, confirming the family rule that unit sizes are not derivable from
the decompiled body.

## Deadness (verified 2026-09-19, tools/deadness_scan.py)

- 0 jal/j/relative-branch targets for 0x12E2F8 across core.text
  (0x112380+0x1D0B8) and .text (0x1E8D00+0x54660); 0 32/64-bit words
  equal to it in any non-text section.
- Whole-ELF raw word search for the jal word
  0x0C000000 | (0x12E2F8 >> 2): zero hits.
- Ghidra (headless): no function at or containing 0x12E2F8; zero xrefs.
- Scanner sanity: on the live sibling 0x12E308 it finds exactly the 2
  jal xrefs Ghidra reports (0x22D660, 0x22EB94).

## Escalation

**last-resort GPT-5.6 Sol used (2026-09-19)** on this exact target (full
dossier: bytes, layout, deadness scans, family + Deadlocked evidence,
t1-t10/p1-p5 probe results, head/sibling verdicts). Verdict: not
regeneratable in acceptable C/C++ with the current EGC toolchain; no
materially distinct recommendation (C form, declaration/signature,
per-TU flag, assembler choice, or Splat boundary) worth probing.
Rationale: the byte-matched parent emits its complete +0x20 epilogue
and no tail (the strongest exact negative test), while the orphan is
two unreachable +0x30 stack adjustments that EGC cannot derive from any
live C operation without altering the parent or adding a terminator;
inline asm is byte-injection equivalent to the fallback. The noted
989snd_post.o GNU-assembler override is irrelevant (direct addiu/nop
words; neither assembler synthesizes missing compiler RTL).

## Resolution

Retain `INCLUDE_ASM("code/_generated/nonmatchings/989snd/ee/989snd_post",
func_0012E2F8)` to supply the 12 bytes and preserve full-ELF parity.
Blocked as a dead tail, not a function. Remaining family siblings take
the same treatment (deadness verification per sibling via
tools/deadness_scan.py, last-resort per the hard rule, block).

## Resolution (2026-09-24)

Converted to top-level inline asm at the INCLUDE_ASM's source position in
code/989snd/ee/989snd_post.c: the exact `addiu $sp,$sp,N` units plus the
trailing alignment nop, byte-for-byte as the generated assembly. The
INCLUDE_ASM and its blocked.json entry were removed. Deadness re-verified
with tools/deadness_scan.py (0 references). Family table:
notes/989snd_func_0012E078.md.
