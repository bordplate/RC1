# func_00207480 (code/game/menu.cpp, vram 0x00207480, 44 bytes) - MATCHED 2026-09-10

Menu predicate callback. Entry 0x19FF90 of the 19-entry jump table at vram
0x19FF70 (.data). Signature `int func(int x, int y)` (C linkage, unmangled
symbol); the sibling geometry callbacks (`func_00206978(x,y)` ->
menu_pointIsClockwise) establish that the first two callback args are (x, y).
`x` is unused; `y` is the discriminator.

## Semantics

```
if (y >= 0x101) return (D_001413DC ^ 0xF) < 1;   // i.e. D_001413DC == 0xF
return D_0013D3BD != 0;
```

- `D_0013D3BD` (core.data 0x13D3BD, u8): per-item enabled-flag byte in the
  0x13D394 family. Specific item unconfirmed.
- `D_001413DC` (core.data 0x1413DC, u32): state word checked for == 0xF.
  Purpose unconfirmed.
- Both are outside the gp window (gp=0x166C00), so EGC emits absolute lui/lw.
  Both share the base `lui v1, 0x14` (D_0013D3BD low16 0xD3BD >= 0x8000 carries
  the hi to 0x14; D_001413DC hi is 0x14 directly).

## The match-critical form

The original is a leaf (no frame):

```
slti  a1, a1, 0x101
beqz  a1, .Lelse        ; fall-through = IF body
  lui   v1, 0x14        ; delay slot
lui   v1, 0x14
lbu   v0, D_0013D3BD(v1)
jr    ra
  sltu  v0, zero, v0
.Lelse:
lw    v0, D_001413DC(v1)
xori  v0, v0, 0xF
jr    ra
  sltiu v0, v0, 1
```

The IF body (2 insns: lbu; sltu) is in the FALL-THROUGH and the 3-insn else is
the beqz target. EGC 2.95.2 (-fno-schedule-insns, menu.cpp flag) reproduces this
ONLY when the condition is written NEGATED:

```cpp
if (y >= 0x101)
    return (D_001413DC ^ 0xF) < 1;
return D_0013D3BD != 0;
```

The natural `if (y < 0x101) return D_0013D3BD != 0; return (D_001413DC ^ 0xF) < 1;`
emits `bnez a1, <if-body>` with the else in the fall-through (block swap + branch
sense flip) - 7 of 11 words differ. With the negated form, EGC computes
`(y < 0x101)` via slti and branches with beqz when it is false, landing the
2-instruction body in the fall-through exactly as the original.

## Verification

- `decomp_probe.py` (flags=-fno-schedule-insns, --define D_0013D3BD=0x13D3BD
  D_001413DC=0x1413DC): 44/44 bytes, 0 differences.
- `make clean && make split && make -j2` + `cmp build/boot_elf.elf
  assets/boot_elf.elf`: pass.
- `python3 tools/decomp_status.py --count`: 732 -> 731.
- decomp-verifier subagent: clean build OK, function slice byte-identical,
  no func_00207480.NON_MATCHING symbol, cmp pass.
