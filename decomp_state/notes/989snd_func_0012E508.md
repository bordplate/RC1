# func_0012E508 (0x12E508, 60 bytes) — MATCHED as dead tail via inline asm (2026-09-24)

NOT a function. Unreachable 60-byte fragment immediately after the
byte-matched parent `snd_SetSoundParams_CB` (0x12E4C0, 48 bytes,
frame 0x30, code/989snd/ee/989snd_post.c:101). Tenth member of the
15-fragment 989snd dead-tail family; family analysis and cross-game
evidence in notes/989snd_func_0012E078.md.

```
0x12E508: addiu sp,sp,0x20   (0x27BD0020)
0x12E50C: nop                (0x00000000)
0x12E510: addiu sp,sp,0x20   (0x27BD0020)
0x12E514: nop                (0x00000000)
0x12E518: addiu sp,sp,0x20   (0x27BD0020)
0x12E51C: nop                (0x00000000)
0x12E520: addiu sp,sp,0x20   (0x27BD0020)
0x12E524: nop                (0x00000000)
0x12E528: addiu sp,sp,0x20   (0x27BD0020)
0x12E52C: nop                (0x00000000)
0x12E530: addiu sp,sp,0x20   (0x27BD0020)
0x12E534: nop                (0x00000000)
0x12E538: addiu sp,sp,0x10   (0x27BD0010)
0x12E53C: nop                (0x00000000)
0x12E540: addiu sp,sp,0x10   (0x27BD0010)
(0x12E544: nop  (alignment padding, OUTSIDE the fragment))
```

Eight units `addiu sp,sp,N` (0x20 x6, then 0x10 x2; the last unit has
no trailing nop within the 60-byte range, and its +0xE0 total has no
corresponding allocation anywhere). The parent completes its balanced
epilogue at 0x12E504 (`lq ra,32(sp)` at 0x12E4FC, `jr ra` at 0x12E500,
`addiu sp,sp,0x30` in the delay slot). The next function
`snd_SendIOPCommandAndWait` starts 8-aligned at 0x12E548
(`addiu sp,sp,-0x70; sq s0,16(sp); ...`). The fragment has no
prologue, no return, no branch — it cannot be a function, and nothing
reaches it. Unit sizes do not correspond to the parent's frame (0x30)
in any derivable way (the family note established unit sizes are not a
function of the decompiled body).

## Deadness (verified 2026-09-19, tools/deadness_scan.py)

- 0 jal/j/relative-branch targets and 0 32/64-bit data words for
  0x12E508 across core.text (0x112380+0x1D0B8), .text
  (0x1E8D00+0x54660), and all data sections.
- Scanner sanity: the same scanner finds the live xrefs on both
  neighbors (0x12E4C0 snd_SetSoundParams_CB: jal at 0x21658C and
  0x22D61C; 0x12E548 snd_SendIOPCommandAndWait: jal at 0x12F12C,
  0x12F160, 0x12F188), so the 0 for 0x12E508 is real.
- Ghidra (headless): no function at or containing 0x12E508; zero xrefs.

## Escalation

**last-resort GPT-5.6 Sol used (2026-09-19)** on this exact target
(full dossier: raw words, parent/next-function boundaries, deadness
scans with neighbor sanity checks, family + Deadlocked .mdebug
evidence, t1-t10/p1-p5 probe results, prior sibling verdicts, GNU/SN
assembler note). Verdict: no credible C/C++, declaration, qualifier,
per-TU flag, or assembler route remains. The matched parent emits its
complete +0x30 restoration at 0x12E504 and the fragment holds eight
further unreachable restorations with no allocation, entry path,
branch, or terminator; EGC deletes unreachable statements and emits
only reachable frame restorations; signatures/qualifiers can touch the
parent's live 48 bytes but cannot create eight post-`jr` adjustments
while preserving them; scheduling/address-splitting flags only
transform existing RTL; GNU vs SN assembly is irrelevant (direct
addiu/nop words, not pseudos); a standalone function needs a
terminator; alignment fill cannot produce six 0x27BD0020 units plus
two 0x27BD0010 units. Explicit byte injection (naked/top-level
asm/.word/inline asm) is the fallback under another spelling, not a
decompilation. No testable recommendation returned.

## Resolution

Retain `INCLUDE_ASM("code/_generated/nonmatchings/989snd/ee/989snd_post",
func_0012E508)` to supply the 60 bytes and preserve full-ELF parity.
Blocked as a dead tail, not a function. Remaining family siblings take
the same treatment (deadness verification per sibling via
tools/deadness_scan.py, last-resort per the hard rule, block).

## Resolution (2026-09-24)

Converted to top-level inline asm at the INCLUDE_ASM's source position in
code/989snd/ee/989snd_post.c: the exact `addiu $sp,$sp,N` units plus the
trailing alignment nop, byte-for-byte as the generated assembly. The
INCLUDE_ASM and its blocked.json entry were removed. Deadness re-verified
with tools/deadness_scan.py (0 references). Family table:
notes/989snd_func_0012E078.md.
