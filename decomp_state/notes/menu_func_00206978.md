# func_00206978 (code/game/menu.cpp, vram 0x00206978, file 0x1078F8, 40 bytes)

Menu hit-test wrapper: forwards its two int args to `func_00208818` together
with four fixed coordinates:

```cpp
extern "C" int func_00208818(int a, int b, int c, int d, int e, int f);

extern "C" int func_00206978(int x, int y) {
    return func_00208818(x, y, 0xD3, 0xDB, 0x129, 0xF9);
}
```

## Semantics / context

- `func_00208818` (vram 0x208818, still INCLUDE_ASM) is a 6-int orientation /
  cross-product sign test. EGC passes args 5 and 6 in `t0`/`t1` (caller-saved
  "hidden args"), which is why every call site (this fn and ~10 others in the
  0x2069xx-0x206Dxx menu callbacks) loads constants into a2/a3/t0/t1 right
  before the `jal`. Body:
  `(e-c)*(b-d) - (f-d)*(a-c) < 0` (side-of-edge test for menu geometry).
- Sibling callbacks differ only in the four constants; func_00206978 uses
  (0xD3, 0xDB, 0x129, 0xF9).
- Ghidra has no function boundary at 0x206978 (folded menu-handler region);
  its identity comes from the generated assembly and the call-site pattern.

## Verification (mechanical)

- Object check: `menu.o` symtab `func_00206978` plain `T` at `.text+0x0`,
  next defined symbol `func_002069A0` at `.text+0x28` -> st_size 0x28; no
  `.NON_MATCHING` alias; only reloc is R_MIPS_26@0x14 against func_00208818.
- The EE toolchain encodes the final `jal` with the 0x30000000 physical
  window bit (word 0x0c082206 = (0x30000000 | 0x208818) >> 2), identical to
  the .s-included path (verified against matched snd_CloseMovieSound).
- Full `make` + `cmp build/boot_elf.elf assets/boot_elf.elf` passes.
- decomp_status active count 833 -> 832.

## Findings that block the sibling candidates (do not retry blindly)

EGC v2.73a (project flags -G8 -O2 -ffast-math -fno-exceptions) consistently
disagrees with the original compiler on two tie-breaks seen in this region:

1. **Branch delay-slot fill**: for `if (cond) g = 3;` shapes
   (func_00208E68/E90/EB8/ED8/F00 family, 32-36B, void, one store to
   D_0015EEB0 after a lui/and/lw test), the original puts `addiu $v0,$0,3`
   in the branch delay slot and `lui at,%hi(D_0015EEB0)` after the branch;
   EGC puts the `lui` in the delay slot and the `li` after. Also, for the
   D_0015EEB4 variant the original keeps base+value in $v0
   (`lui v0; lw v0,lo(v0)`) while EGC keeps the base live in $v1. Tried 7
   source shapes (if/cond orders, named temps, early-return, ternary) - all
   land on the EGC schedule. D_0015EEB0/EEB4 are dlabels in section `.lit4`
   (code/_generated/build/data/core.lit.lit4.s); `extern int X
   __attribute__((section(".lit4")));` is required to get absolute %hi/%lo
   (bare extern gives gp-relative lw/sw under -G8).

2. **func_00208818**: with the correct 6-arg C signature the register
   allocation matches (a-c in a0, b-d in a1, e-c in v0, f-d in t1) but EGC
   -O2 schedules the four independent subus as [v0,t1,a0,a1] vs original
   [a0,a1,v0,t1] (a fixed source-order rotation by 2, invariant across all
   tried expression orderings), and compiles `< 0` to `srl v0,v0,31` instead
   of the original `slti v0,v0,0`. -O1 reproduces the subu order but -O1
   callers (e.g. func_00206978) then mis-schedule `sq ra` (hoisted above the
   constant loads), while the original caller matches -O2 exactly - so the
   original binary is -O2 and this function is a toolchain tie-break
   mismatch. Recorded in blocked.json.
