# func_0012E270 (0x12E270, 12 bytes) — MATCHED as dead tail via inline asm (2026-09-24)

NOT a function. Unreachable 16-byte gap immediately after the
byte-matched parent `snd_SetPlaybackMode` (0x12E240, 0x30 bytes,
frame 0x20, code/989snd/ee/989snd_post.c:32). Third member of the
15-fragment 989snd dead-tail family; family analysis, cross-game
(Deadlocked) evidence, and probe results in
notes/989snd_func_0012E078.md.

```
0x12E270: addiu sp,sp,0x10   (0x27BD0010)
0x12E274: nop                (0x00000000)
0x12E278: addiu sp,sp,0x10   (0x27BD0010)
0x12E27C: nop  (alignment padding, OUTSIDE Splat's endlabel; Splat
               declares the fragment 0xC bytes)
```

The parent completes its balanced epilogue at 0x12E26C (`jr ra` at
0x12E268, `addiu sp,sp,0x20` in the delay slot). The next function
`snd_SetMixerMode` (matched, 0x12E280) starts 8-aligned after the gap.
The fragment has no prologue, no return, no branch — it cannot be a
function. Unit sizes (0x10, 0x10) are half the parent's 0x20 frame;
the family note established unit sizes are not derivable from the
decompiled body.

## Deadness (verified 2026-09-19, tools/deadness_scan.py)

- 0 jal/j targeting 0x12E270 in core.text (0x112380+0x1D0B8) or
  .text (0x1E8D00+0x54660).
- 0 relative branches (beq/bne/blez/bgtz/beql/bnel/blezl/bgtzl)
  targeting 0x12E270.
- 0 32/64-bit words equal to 0x12E270 in any non-text section.
- Ghidra (headless): no function at or containing 0x12E270; zero xrefs.
- Scanner sanity: adjacent live symbol 0x12E1A8 still finds exactly
  the 6 jal xrefs Ghidra reports; sibling 0x12E198 still 0 references.

Scanner fix (committed separately, 2026-09-19): the pre-fix revision
used branch major opcodes 12-15, which are andi/ori/xori/lui on
MIPS/R5900, and produced a FALSE POSITIVE here (a `lui v1,0x1000` at
0x12A26C computed target 0x12E270). The real beql/bnel/blezl/bgtzl
opcodes are 20-23 (640 instances in the boot ELF, previously unscanned).
Results above are from the corrected scanner.

## Escalation

**last-resort GPT-5.6 Sol used (2026-09-19)** on this exact target
(full dossier: bytes, layout, deadness scans, family + Deadlocked
evidence, t1-t10/p1-p5 probe results, family-head verdicts). Verdict:
the blocker is MISSING RTL, not scheduling or register allocation —
no credible C/C++ form, declaration, qualifier, per-TU compiler flag,
or assembler setting remains to test. The matched parent is the
strongest exact test: it compiles all 48 bytes of snd_SetPlaybackMode
and emits no tail (local EGC never emits post-return sp-restoration
RTL in the tested forms); a standalone C function must emit a
terminator, and folding into snd_SetMixerMode is impossible since all
callers enter at 0x12E280, whose matched prologue begins there.
Exotic forms (noreturn, explicit $sp register variables, naked
functions, custom alignment fill, top-level asm, initialized .text
words) either still emit control flow, alter the live parent, are
unsupported, or are byte-injection equivalent to the INCLUDE_ASM
fallback. No materially distinct probe remains.

## Resolution

Retain `INCLUDE_ASM("code/_generated/nonmatchings/989snd/ee/989snd_post",
func_0012E270)` to supply the bytes and preserve full-ELF parity.
Blocked as a dead tail, not a function. Remaining family siblings take
the same treatment (deadness verification per sibling via
tools/deadness_scan.py, last-resort per the hard rule, block).

## Resolution (2026-09-24)

Converted to top-level inline asm at the INCLUDE_ASM's source position in
code/989snd/ee/989snd_post.c: the exact `addiu $sp,$sp,N` units plus the
trailing alignment nop, byte-for-byte as the generated assembly. The
INCLUDE_ASM and its blocked.json entry were removed. Deadness re-verified
with tools/deadness_scan.py (0 references). Family table:
notes/989snd_func_0012E078.md.
